[homelab]
gpu-node ansible_host=192.168.0.102

[homelab:vars]
ansible_user=ubuntu
# SSH and sudo passwords are NOT stored here — they are prompted at run time
# via `--ask-pass` (SSH) and `--ask-become-pass` (sudo). See the run command below.
